name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: "npm"

      - name: "Debug - runner info"
        run: |
          echo "Runner: $(uname -a || hostnamectl || echo unknown)"
          node -v || echo "node not found"
          npm -v || echo "npm not found"
          ls -la

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build -- --configuration production --base-href /Angular-Project/

      - name: Create .nojekyll using script
        run: node ./scripts/create-nojekyll.js

      - name: "Debug - Verify build output"
        run: |
          echo 'Listing dist/project:'
          ls -la dist/project || echo "dist/project not found"
          echo 'Checking if .nojekyll exists:'
          ls -la dist/project/.nojekyll || echo ".nojekyll not found"

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist/project

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
